# Spring Boot backend. Must be NON-EMPTY - an empty value is falsy in JS
# and api.js silently falls back to http://localhost:8080.
VITE_API_BASE_URL=http://localhost:8080

# Optional: Google OAuth Web client ID to enable the Google button.
VITE_GOOGLE_CLIENT_ID=
